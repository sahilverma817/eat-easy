# Get from Vercel KV / Upstash Redis (auto-injected on Vercel)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Get from https://aistudio.google.com/apikey (free)
GEMINI_API_KEY=

# Random 32-byte hex (run: openssl rand -hex 32)
SESSION_SECRET=
